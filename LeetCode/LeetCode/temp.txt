//cpp.code-snippets
{
	"C++ for index loop": {
		"scope": "cpp,c",
		"prefix": "for",
		"body": [
			"for (int ${1:i} = ${2:0}; ${1:i} < ${3:n}; ${1:i}++) {",
			"\t$0",
			"}"
		],
		"description": "for (int i = 0; i < n; i++)"
	}  
}
